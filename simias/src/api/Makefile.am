DIST_SUBDIRS = libsimias 

if DARWIN
SUBDIRS = libsimias

endif

if LINUX
SUBDIRS = libsimias

endif

if WINDOWS
SUBDIRS = libsimias

endif

EXTRA_DIST = 


clean-local:
	rm -rf $(COMMON_CLEAN_FILES)

distclean-local:
	rm -rf $(COMMON_DISTCLEAN_FILES)

maintainer-clean-local:
	rm -rf $(COMMON_MAINTAINER_CLEAN_FILES)

