See http://www.ifolder.com/ for more information on the iFolder open source project.

AUTHOR: SATYANARAYANA SVV
PRODUCT: Plugins for Novell-iFolder
Description: All the Windows DLL's must be signed with iFolder.snk which is available in src/WindowsClient directory. 

To build server plugins run : "autogen.sh --prefix=/path/of/simias"

To build client plugins run : "autogen.sh --prefix=/path/of/simias --with-clientplugins"

For building server plugins, simias needs to built/installed at /path/of/simias. The server plugins will be moved to simias repository soon.
