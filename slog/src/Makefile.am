if LINUX
SUBDIRS = Slog GtkSlogger
endif

if WINDOWS
SUBDIRS = Slog
endif

DIST_SUBDIRS = 

EXTRA_DIST = 

install-exec-local:

uninstall-local:

installcheck-summary:

clean-local:
    
distclean-local:

maintainer-clean-local:

