{
    IBClasses = (
        {
            ACTIONS = {accept = id; decline = id; showSheet = id; }; 
            CLASS = AcceptCertSheetController; 
            LANGUAGE = ObjC; 
            OUTLETS = {certBox = NSBox; certScrollView = NSScrollView; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}